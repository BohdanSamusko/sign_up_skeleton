name: data
publish_to: 'none'
description: Data layer of the application.

environment:
  sdk: ^3.4.1

# Add regular dependencies here.
dependencies:
  #Local dependencies
  domain:
    path: ../domain
  core:
    path: ../core

  get_it: ^8.0.2
  injectable: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2
  flutter_lints: ^5.0.0
